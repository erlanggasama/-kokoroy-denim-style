{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "pnpm -r --filter=./apps/web run build",
    "typecheck": "pnpm -r run typecheck",
    "dev": "pnpm -r --filter=./apps/web run dev"
  },
  "devDependencies": {
    "prettier": "^3.8.4",
    "typescript": "^5.9.3"
  }
}